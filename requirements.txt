# Smart Vision Guide - Dependencies
# Optimized for Raspberry Pi OS Buster 10 (Debian Buster, Python 3.7.3)

requests>=2.22.0
Pillow>=6.0.0,<10.0.0
python-dotenv>=0.20.0
gTTS>=2.3.0
RPi.GPIO>=0.7.1; platform_machine=='armv6l' or platform_machine=='armv7l' or platform_machine=='aarch64'

# OpenCV is installed via apt (python3-opencv) in setup_system.sh
# to avoid long compilation times on Pi Zero 2W.

# Face recognition: Using OpenCV LBPH (installed via apt as python3-opencv)
# This avoids dlib compilation which crashes Pi Zero 2W due to memory limits
# The cv2.face.LBPHFaceRecognizer is lightweight and works offline
