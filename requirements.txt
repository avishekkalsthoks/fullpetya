# Smart Vision Guide - Dependencies
# Optimized for Raspberry Pi OS Buster 10 (Debian Buster, Python 3.7.3)

requests>=2.22.0
Pillow>=6.0.0,<10.0.0
python-dotenv>=0.20.0
gTTS>=2.3.0
RPi.GPIO>=0.7.1; platform_machine=='armv6l' or platform_machine=='armv7l' or platform_machine=='aarch64'

# OpenCV - use headless version for better ARM compatibility
opencv-python-headless>=4.1.0,<5.0.0; platform_machine!='armv6l'

# Face recognition dependencies
# Note: dlib may require compilation on Buster 10
# Install system deps: sudo apt-get install cmake build-essential libatlas-base-dev
dlib>=19.17.0
face-recognition>=1.3.0
